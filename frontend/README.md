# Frontend (React + Vite)

## Démarrage rapide

```bash
cd frontend
npm install
npm run dev
```

Par défaut, le front appelle la gateway sur `http://localhost:3000`.
Pour changer :

```bash
cp .env.example .env
```

Puis modifie `VITE_API_URL`.
